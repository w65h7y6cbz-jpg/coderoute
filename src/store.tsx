import {createContext,useContext,useEffect,useState,type ReactNode} from 'react';import type {AppState,Exam,Question} from './types';import {updateAfterAnswer} from './lib/engine';
const in7=new Date(Date.now()+7*86400000).toISOString().slice(0,10);const initial:AppState={mastery:{},exams:[],days:[],xp:0,streak:0,examDate:in7,sound:false,animations:true,threshold:35};
type Ctx={state:AppState;answer:(q:Question,ok:boolean)=>void;saveExam:(e:Exam)=>void;patch:(p:Partial<AppState>)=>void;reset:()=>void;importData:(s:AppState)=>void};
const Context=createContext<Ctx|null>(null);export function Store({children}:{children:ReactNode}){const [state,setState]=useState<AppState>(()=>{try{return {...initial,...JSON.parse(localStorage.getItem('cap-code-v1')||'{}')}}catch{return initial}});useEffect(()=>localStorage.setItem('cap-code-v1',JSON.stringify(state)),[state]);return <Context.Provider value={{state,answer:(q,ok)=>setState(s=>updateAfterAnswer(s,q,ok)),saveExam:e=>setState(s=>({...s,exams:[e,...s.exams].slice(0,30)})),patch:p=>setState(s=>({...s,...p})),reset:()=>setState(initial),importData:setState}}>{children}</Context.Provider>};export const useStore=()=>{const c=useContext(Context);if(!c)throw Error('Store missing');return c};
